const {Console} = require("console-mpds");
const console = new Console();

const num = console.readNumber("Introduce el numerador de la fracción:");
const den = console.readNumber("Introduce el denominador de la fracción:");

console.writeln(`la fracción ${num}/${den} invertida, es la fracción ${den}/${num}`);